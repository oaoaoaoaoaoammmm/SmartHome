spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: off
  data:
    web:
      pageable:
        max-page-size: 20

server:
  port: 8081
  servlet:
    context-path: /api
